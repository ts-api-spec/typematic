{
  "name": "typematic",
  "version": "1.0.0",
  "description": "typescript api spec",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "test:watch": "turbo run test:watch",
    "test:coverage": "turbo run test:coverage",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "pub": "turbo run build test && changeset version && changeset publish"
  },
  "private": true,
  "keywords": [
    "openapi"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@changesets/cli": "^2.26.2",
    "turbo": "^1.10.12"
  }
}